{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container mt-5 pt-5" style="margin-left: 10%;">
    <div class="card p-4 shadow-lg mx-auto" style="max-width: 1000px;">
        <h1 class="text-center mb-4 text-warning">Edit User</h1>

        {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="text-center mt-4">
            <a href="{{ path('app_user_index') }}" class="btn btn-secondary">Back to List</a>
        </div>

        <div class="mt-3 text-center">
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
